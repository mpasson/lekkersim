image: python:3.6-alpine

pages:
  stage: deploy
  script:
  - mkdir public
  - pip install sphinx
  - pip install sphinx-rtd-theme
  - sphinx-build -b html Docs public/
  artifacts:
    paths:
    - public
  only:
  - master


